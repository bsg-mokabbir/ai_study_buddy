"""
Test package for AI Study Buddy.

This package contains all unit tests and integration tests for the AI Study Buddy application.
"""

import sys
import os

# Add the parent directory to the path so we can import from utils, templates, etc.
sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))
